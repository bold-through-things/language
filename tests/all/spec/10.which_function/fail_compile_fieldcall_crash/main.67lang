type named Heartbeat is heap_entry
	has heartbeat_interval
		type named int
	has sequence_number
		type named int

local heartbeat
	type named Heartbeat
	pls get Heartbeat
		int 0
		int 0

note this one also fails as i nuked the `data`
pls get # from event as data
	string "data"

note that `fn` is not a closure, which means that it does not
	see the `heartbeat`
fn send_heartbeat
	returns
		type named void
	pls get sequence_number from heartbeat
