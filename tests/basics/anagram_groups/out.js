(() => {
  let input = "stdin";
  let words = input.split("\n");
  let groups = {};
  for (const word of words) {
    let key = word.split("where split takes");
    if ("none") {
  }
  }
  for (const group of "values") {
    console.log(group.join(" "));
  }
})();