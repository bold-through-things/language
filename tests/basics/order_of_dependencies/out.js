(() => {
  let lines = "stdin";
  let modules = {};
  for (const line of "a lines") {
    let module = {};
    let kv = line.split(":");
    if (("a kv 1 trim" === "")) {
  }
  }
  let build_order = [];
  let dep_loops = [];
  for (const module of "values") {
  }
  if ((1 < "a dep_loops length")) {
    console.log("concat");
  } else {
    console.log(build_order.join("\n"));
  }
})();