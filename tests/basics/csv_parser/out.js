let lines = stdin;
lines(lines split("\n"))
let i = 0;
let header = [];
let rows = [];
for (const line of ) {
    if ((access i === 0)) {
    header(line split(","))
  } else {
    let zip = zip;
    let row = {};
    for (const kv of ) {
    row[kv[0]] = kv[1];
  }
    rows.push
  }
    then
    else
    i((access i + 1))
  }
for (const row of ) {
    console.log(row.name);
  }
let age_over_30 = 0;
for (const row of ) {
    if ((row.age < 30)) {
    age_over_30((access age_over_30 + 1))
  }
    then
  }
console.log(access age_over_30);