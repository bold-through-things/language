local myURL
	a URL
		string "https://example.com/path?query=123"

a print
	a myURL pathname

local encoder
	a TextEncoder

local encoded
	an encoder encode
		string "hello world"

a print
	an encoded

local decoder
	a TextDecoder

local decoded
	a decoder decode
		an encoded

a print
	a decoded