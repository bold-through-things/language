string
	this should fail due to string != int

local .name
.name
	int 0

print
	eq
		.name
		string ""

string ! TODO - not really a typecheck, but still need this !

local name
name
	string !this should fail because name is not a macro, it should start with a period!

debug
